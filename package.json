{
  "name": "@projectrose/rose-qwen-director",
  "version": "1.0.0",
  "description": "Director extension — keeps the agent on-task by tracking checklists in its thinking.",
  "private": true,
  "exports": {
    "./main": "./main.ts"
  },
  "scripts": {
    "build:main": "esbuild main.ts --bundle --format=cjs --platform=node --outfile=dist/main.js --external:electron --external:@main/*",
    "build": "npm run build:main"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "typescript": "^5.0.0"
  }
}
